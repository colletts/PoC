<blueprint xmlns="http://www.osgi.org/xmlns/blueprint/v1.0.0"
           xmlns:camel="http://camel.apache.org/schema/blueprint"
           xmlns:cxf="http://camel.apache.org/schema/blueprint/cxf"
    >

    <bean id="defaultErrorHandler" class="org.apache.camel.builder.DefaultErrorHandlerBuilder">
    </bean>

    <bean id="retryErrorHandler" class="org.apache.camel.builder.DefaultErrorHandlerBuilder">
        <property name="redeliveryPolicy" ref="retryPolicy"/>
    </bean>

    <bean id="retryPolicy" class="org.apache.camel.processor.RedeliveryPolicy">
        <property name="maximumRedeliveries" value="-1"/>
        <property name="retryAttemptedLogLevel" value="WARN"/>
    </bean>

    <property-placeholder persistent-id="org.fusesource.camel.ws"
                          xmlns="http://aries.apache.org/blueprint/xmlns/blueprint-cm/v1.0.0">
        <default-properties>
            <property name="server.de.20" value="http://localhost:8184" />
        </default-properties>
    </property-placeholder>


    <cxf:rsServer id="restServer"
                  address="${server.de.20}/cxf/initiateOfflineRequest"
                  serviceClass="org.poc.rs.InitiateEndpoint" />

    <camelContext id="de20" errorHandlerRef="defaultErrorHandler" xmlns="http://camel.apache.org/schema/blueprint">
        <route id="restInputRoute">
            <from uri="cxfrs:bean:restServer"/>
            <log message="Received rest msg for de20"/>
            <setBody>
                <constant>message</constant>
            </setBody>
            <to uri="activemq:connectToOfflineEndpoint" />
            <to uri="direct:offlineEndpoint" />
        </route>

        <route id="connectToOfflineEndpointRoute">
            <from uri="activemq:connectToOfflineEndpoint"/>
            <log message="Get message from queue for de20"/>
            <to uri="vm:bar"/>
        </route>

        <route errorHandlerRef="retryErrorHandler">
            <from uri="direct:offlineEndpoint"/>
            <log message="Get message from direct for de20"/>
            <to uri="vm:bar"/>
        </route>

    </camelContext>

    <!-- Set username and password to values you set in etc/user.properties -->
    <bean id="activemq" class="org.apache.activemq.camel.component.ActiveMQComponent">
        <property name="brokerURL" value="tcp://localhost:61616" />
        <property name="userName" value="admin" />
        <property name="password" value="admin" />
    </bean>

</blueprint>